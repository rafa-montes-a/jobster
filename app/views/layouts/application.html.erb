<!DOCTYPE html>
<html>
  <head>
    <title>Jobster</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

    <link rel="stylesheet"href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.css">
				
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>

    <style>

      .banner{
        display: flex;
        padding-left: 100px;
        padding-right: 0px;       
      }    

      .right{
        margin-left: auto;
      }

    </style>

  </head>

  <body>

<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Jobster</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">

      <div class="navbar-nav">        
        <% if @current_user == nil %>
            <div>Welcome!</div>
        <% else %>
          <a class="nav-link" href="/jobs">My applications</a>
          <a class="nav-link" href="/contacts">My contacts</a>
          <a class="nav-link" href="/firms">My companies</a>
          <a class="nav-link" href="/touchpoints">Touchpoints</a>
          <a class="nav-link" href="/questions">Questions</a>
        <% end %>
      </div>

      <div class="navbar-nav right">
        <% if @current_user == nil %>        
          <a class="nav-link" href="/user_sign_in">Log in</a>
          <a class="nav-link" href="/user_sign_up">Create account</a>

        <% else %>
          <a class="nav-link" href="/edit_user_profile">Hi, <%= @current_user.first_name %> </a>
          <a class="nav-link" href="/user_sign_out">Log out</a>
        <% end %>

      </div>
    </div>
  </div>
</nav>

  <div class = "container py-5">
    <p style = "color:green"><%= notice %></p>
    <p style = "color:red"><%= alert %></p>
    <%= yield %>
  </div>

  </body>
</html>
